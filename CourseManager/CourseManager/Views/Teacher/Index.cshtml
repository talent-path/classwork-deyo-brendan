@model IEnumerable<Teacher>
@{
}

<table>
    <p>Teachers:</p> 
    @foreach (Teacher t in Model)
    {
        <tr>
            <td>@Html.ActionLink(t.Name, "Details", new { id = t.Id })</td>
            <td>@Html.ActionLink("Edit", "Edit", new { id = t.Id })</td>
            <td>@Html.ActionLink("Delete", "Delete", new { id = t.Id })</td>
        </tr>
    }

</table>
